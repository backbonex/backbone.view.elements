define(['lib/Backbone.View.Elements'], function (ElementsView) {
    /**
     * @class ListView
     * @extends ElementsView
     */
    var ListView = ElementsView.extend(/**@lends ListView*/{
        events: function () {

        }
    });
});
